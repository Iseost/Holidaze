import { useEffect, useState } from "react";
import { getUserProfileWithBookings, updateAvatar, updateBanner } from "../api/profiles.mjs";

export default function ProfileCustomer() {
    const [profile, setProfile] = useState(null);
    const []
} 